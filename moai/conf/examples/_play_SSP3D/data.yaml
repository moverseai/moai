# @package _global_

SSP3D_root: ???

data:
  train:
    loader:
      drop_last: true
    iterator:
      datasets:
        SSP3D:
          betas: ${BETAS}
          with_openpose: true
  test:
    loader:
      drop_last: true
    iterator:
      datasets:
        SSP3D:
          betas: ${BETAS}
          with_openpose: true
  val:
    loader:
      drop_last: true
    iterator:
      datasets:
        SSP3D:
          betas: ${BETAS}
          with_openpose: true