# @package _global_

amass_root: ???
smplx_root: ???

data:
  train:    
    iterator:      
      datasets:
        AMASS:
          parts: ['ACCAD']
          downsample_factor: 2
          reconstruct: true
  test:
    iterator:
      datasets:
        AMASS:
          parts: ['SOMA']
          downsample_factor: 5
          reconstruct: true
  val:
    iterator:
      datasets:
        AMASS:
          parts: ['SOMA']
          downsample_factor: 10
          reconstruct: true