# @package _global_

model:
  monads:
    clamp:
      max_value: 10.0
    threshold:
      value: 15.0

  feedforward:
    preprocess:
      threshold:
        x: [left_down_depth]
        out: [left_down_mask]
      clamp:
        x: [left_down_depth]
        out: [left_down_depth]