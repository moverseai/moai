# @package _global_

data:
  train:
    iterator:
      datasets:
        mnist:
          root: ${DATA_ROOT}
          train: true
          download: true
  val:
    iterator:
      datasets:
        mnist:
          root: ${DATA_ROOT}
          train: false
          download: true
  test:
    iterator:
      datasets:
        mnist:
          root: ${DATA_ROOT}
          train: false
          download: true
