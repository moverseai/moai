# @package data.val.iterator.augmentation.augmentations._name_

_target_: albumentations.augmentations.transforms.Posterize
num_bits: 4 # ((int, int) or int, or list of ints [r, g, b], or list of ints [[r1, r1], [g1, g2], [b1, b2]]): number of high bits. If num_bits is a single value, the range will be [num_bits, num_bits]. Must be in range [0, 8]. Default: 4.
p: 0.5 #probability