# @package data

train:
  loader:
    batch_size: 32
    num_workers: 4
  iterator:
    datasets:
      cifar10:
        download: true

test:
  loader:
    batch_size: 32
    num_workers: 1
  iterator:
    datasets:
      cifar10:
        download: true